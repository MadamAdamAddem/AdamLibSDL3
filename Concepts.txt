Root node is a singleton.
Node name is simply the name with no preceeding or ending /'.
Node global path is the path to the node starting with '/root' and ending with '/<name>'.

Node Naming Conventions:
  -Must not contain a '/' character
  -Must not match sibling
  -If name matches sibling, name is appended with a . 